name: Playwright Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  test:
    runs-on: ubuntu-latest

    permissions:
      checks: write
      pull-requests: write
      contents: read

    steps:
      - name: Checkout code
        uses: actions/checkout@v4.2.2 # ✅ Actualiza a versión más reciente

      - name: Set up Python
        uses: actions/setup-python@v5.2.0 # ✅ Actualiza a versión más reciente
        with:
          python-version: "3.11"

      - name: Set up Node.js
        uses: actions/setup-node@v4.1.0 # ✅ Actualiza a versión más reciente
        with:
          node-version: "24"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Install Playwright browsers
        run: playwright install chromium

      - name: Run tests
        run: pytest tests/test_login.py -v --alluredir=allure-results --junitxml=test-results.xml
        continue-on-error: true

      - name: Upload Allure results
        uses: actions/upload-artifact@v4.4.3 # ✅ Actualiza a versión más reciente
        if: always()
        with:
          name: allure-results
          path: allure-results/

      - name: Publish test results
        uses: EnricoMi/publish-unit-test-result-action@v2
        if: always()
        with:
          files: test-results.xml
          check_name: "Playwright Test Results"
